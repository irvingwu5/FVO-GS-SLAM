## SLAM parameters
Results:
  save_results: True
  save_dir: "ablation_results"
  save_trj: True
  save_trj_kf_intv: 5
  use_gui: True
  eval_rendering: False
  use_wandb: False

Dataset:
  type: 'tum'
  sensor_type: 'depth'
  pcd_downsample: 64
  pcd_downsample_init: 32
  adaptive_pointsize: True
  point_size: 0.01
  use_label: True

Training:
  #Initialization
  init_itr_num: 1050
  init_gaussian_update: 100
  init_gaussian_reset: 500
  init_gaussian_th: 0.005
  init_gaussian_extent: 30
  # Tracking and Mapping
  tracking_itr_num: 150 #100->200
  mapping_itr_num: 150 #150-->250
  gaussian_update_every: 150
  gaussian_update_offset: 50
  gaussian_th: 0.7
  gaussian_extent: 1.0
  gaussian_reset: 2001
  size_threshold: 20 #20->80
  kf_interval: 5 #5->10
  window_size: 10
  pose_window: 10 #3->8
  geo_window_size: 3 #新增，几何优化窗口大小
  use_geo_window: True    # [新增] 是否开启几何优化窗口限制。True: 只优化最近N帧; False: 优化窗口内所有
  edge_threshold: 1.1
  rgb_boundary_threshold: 0.01
  alpha: 0.9
  kf_translation: 0.08
  kf_min_translation: 0.05
  kf_overlap: 0.6 #0.8->0.6
  kf_cutoff: 0.3
  prune_mode: 'slam'
  single_thread: False
  spherical_harmonics: False
  lr:
    cam_rot_delta: 0.003
    cam_trans_delta: 0.001
  sagsslam:
    use_normal: True
    use_plane_constraint: False
    loss_type: "Huber"  # "l1" or "l2"
    warmup_iter: 2500


## 3DGS default parameters
opt_params:
  iterations: 30000
  position_lr_init: 0.00016
  position_lr_final: 0.0000016
  position_lr_delay_mult: 0.01
  position_lr_max_steps: 30000
  feature_lr: 0.0025
  opacity_lr: 0.05
  scaling_lr: 0.001
  rotation_lr: 0.001
  percent_dense: 0.01
  lambda_normal: 0.006 #normal consistency loss weight 0.0001->0.05
  densification_interval: 100
  opacity_reset_interval: 3000 #3000->6000
  densify_from_iter: 500
  densify_until_iter: 15000
  densify_grad_threshold: 0.0004 #0.0002->0.0008

model_params:
  sh_degree: 0
  source_path: ""
  model_path: ""
  resolution: -1
  white_background: False
  data_device: "cuda"

pipeline_params:
  convert_SHs_python: False
  compute_cov3D_python: False
  depth_ratio: 1.0
  # psedo surface attributes
  # surf depth is either median or expected by setting depth_ratio to 1 or 0
  # for bounded scene, use median depth, i.e., depth_ratio = 1;
  # for unbounded scene, use expected depth, i.e., depth_ration = 0, to reduce disk anliasing.
