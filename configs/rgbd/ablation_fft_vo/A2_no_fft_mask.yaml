inherit_from: "configs/rgbd/tum/fr3_office.yaml"

Results:
  eval_rendering: true

# A2: No FFT mask seeding — all Gaussians get uniform initial scale (no freq-guided scaling)
FFTEdgeVO:
  use_fft_edge_vo: true

Ablation:
  use_loop_closure: false
  use_fft_mask: false
