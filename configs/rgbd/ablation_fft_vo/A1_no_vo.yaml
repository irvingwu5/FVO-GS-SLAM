inherit_from: "configs/rgbd/tum/fr3_office.yaml"

Results:
  eval_rendering: true

# A1: No FFT Edge VO — tracking falls back to previous-frame pose + render refinement
VOPrior:
  type: none

Ablation:
  use_loop_closure: false
  use_fft_mask: true
