# Round F: SA only — 验证 Replica base 注释"SA 损害遮挡边界 tracking"
inherit_from: "configs/rgbd/replica/base_config.yaml"

Dataset:
  dataset_path: "/home/wuxiangyu/Desktop/data/2d_gaussian_slam/datasets/replica/room0"

# === 仅 SA depth + SA dist ON ===
Ablation:
  use_fft_mask: False
  use_submap: False
  use_loop_closure: False
  use_fdn: False
  use_error_mask: False

VOPrior:
  type: none

Training:
  use_rskm: False

opt_params:
  use_sa_dist: true
  lambda_dist: 0.005

pipeline_params:
  use_sa: true
  use_sa_depth: true

Backend:
  optimize_keyframe_pose: true
