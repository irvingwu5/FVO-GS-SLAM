# Round A: Replica baseline — 仅 FFT mask ON（当前默认"好"配置）
inherit_from: "configs/rgbd/replica/base_config.yaml"

Dataset:
  dataset_path: "/home/wuxiangyu/Desktop/data/2d_gaussian_slam/datasets/replica/room0"

# === base_config 默认：FFT mask ON, others OFF, kf_pose=true ===
# 无需额外覆盖，显式列出以保持与 B-F 轮一致
Ablation:
  use_fft_mask: True
  use_submap: False
  use_loop_closure: False
  use_fdn: False
  use_error_mask: False

VOPrior:
  type: none

Training:
  use_rskm: False

opt_params:
  use_sa_dist: false
  lambda_dist: 0.005

pipeline_params:
  use_sa: false
  use_sa_depth: false

Backend:
  optimize_keyframe_pose: true
