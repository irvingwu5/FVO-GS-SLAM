# Round D: FFTVO + FFT mask — 测试 FFT mask 是否缓解 FFTVO 退化
inherit_from: "configs/rgbd/replica/base_config.yaml"

Dataset:
  dataset_path: "/home/wuxiangyu/Desktop/data/2d_gaussian_slam/datasets/replica/room0"

# === FFTVO + FFT mask ON ===
Ablation:
  use_fft_mask: True
  use_submap: False
  use_loop_closure: False
  use_fdn: False
  use_error_mask: False

VOPrior:
  type: simple_rgbd_odom

Training:
  use_rskm: False

opt_params:
  use_sa_dist: false
  lambda_dist: 0.005

pipeline_params:
  use_sa: false
  use_sa_depth: false

Backend:
  optimize_keyframe_pose: true
